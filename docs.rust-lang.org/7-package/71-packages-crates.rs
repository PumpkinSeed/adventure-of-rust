fn main() {
    // module system:
    // pacakges: a cargo feature that lets you build, test, and share crates
    // crates: a tree of modules that procuces a library or exacutable
    // modules and use: let you control the organization, scope and privacy of paths
    // paths: a way of naming an item, such as struct, function, or module

    // packages and crates
    // a crate is a binary or a library
    // a package is one or more crates that providing a set of functionalities, contains Cargo.toml

    // creating new package
    // cargo new my-project
    
}